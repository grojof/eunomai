node_modules/
dist/
*.tgz
coverage/

# Ship the self-contained CLI bundle with the plugin (everything else in dist/ stays ignored)
!tools/dist/
tools/dist/*
!tools/dist/cli.cjs

# eunomai safe-controls: personal, per-clone overrides
.claude/eunomai.local.json
